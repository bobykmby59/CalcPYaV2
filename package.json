{
  "name": "rider-calc-pro-gt",
  "version": "3.6.1",
  "description": "App Nativa de Cálculo y GPS para Riders en Guatemala",
  "main": "www/index.html",
  "scripts": {
    "build": "echo 'No build step required'",
    "cap:sync": "npx cap sync",
    "cap:open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/android": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}